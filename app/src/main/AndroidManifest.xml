<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.legrand.daifen.application">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".DFApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.ui.main.MainActivity" />
        <activity android:name=".presentation.ui.login.LoginActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.ui.pigeon.detail.PigeonDetailActivity" />
    </application>
</manifest>
